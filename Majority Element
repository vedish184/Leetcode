class Solution {
    public int majorityElement(int[] nums) {
       int l=nums.length;
       int count=0;
       for(int i=0;i<l;i++){
           for(int j=0;j<l;j++){
               if(nums[i]==nums[j]){
                   count+=1;
               }
           }
       if(count>l/2){
           return nums[i];
       }
       count=0;
       }
       return -1;
    }
}
